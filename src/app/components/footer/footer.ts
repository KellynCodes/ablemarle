import { RouterLink } from '@angular/router';
import { Component } from '@angular/core';

@Component({
  selector: 'ablemarle-footer',
  templateUrl: './footer.html',
  standalone: true,
  imports: [RouterLink],
})
export class Footer {}
